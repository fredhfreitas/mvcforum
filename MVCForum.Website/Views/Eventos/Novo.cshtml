@using MvcForum.Web.Application.ExtensionMethods
@model MvcForum.Web.ViewModels.Topic.CreateEditTopicEventViewModel
@{
    Layout = "~/Views/Shared/_LayoutFullWidth.cshtml";
    ViewBag.Title = "Novo Evento";
}

@section Header
{
    @(
 Bundle.JavaScript()
            .Add("~/Scripts/jquery.tagsinput.min.js")
            .Add("~/Scripts/tags.js")
            .Add("~/Scripts/createtopic.js")
            .MvcRender("~/scripts/cache/topic-combined_#.js")
    )
    @*<script type="text/javascript">
        counter = @(Model.PollAnswers != null ? Model.PollAnswers.Count : 0);
    </script>*@
}
<div class="row default-header">
    <div class="col-md-12">
        <h1 class="title-bar">Novo Evento</h1>
    </div>
</div>
<div class="row sharephoto sharenewtopic">

    <div class="col-md-12">
        <div class="createtopicholder">

            @using (Html.BeginForm("NewEvent", "Topic", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.ValidationSummary(false)
                @Html.AntiForgeryToken()
                @Html.Partial("_CreateEdit_TopicPostEvento", Model)
            }

        </div>
    </div>
</div>
